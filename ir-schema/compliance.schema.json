{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://escript-lang.dev/schemas/compliance-ir.json",
  "title": "EScript compliance IR (guard-first)",
  "description": "Validates IR documents that participate in compliance or guard pipelines. All fields from escript-ir-v1.json remain allowed; this schema adds requirements so tooling can reject empty guard lists.",
  "allOf": [
    {
      "$ref": "escript-ir-v1.json"
    },
    {
      "type": "object",
      "required": ["guards"],
      "properties": {
        "guards": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "escript-ir-v1.json#/$defs/guard"
          }
        }
      }
    }
  ]
}
